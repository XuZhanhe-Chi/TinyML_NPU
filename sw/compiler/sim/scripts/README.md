# Simulation Scripts

Retained scripts:

- `run_behavioral_sim.py`: run one compiled uOP/param bundle through the Python behavioral model.
- `run_plan_sim.py`: run a compiled plan bundle against local input/reference arrays.

These scripts are primarily for local validation. Large testvector arrays are not included in the public tree.
